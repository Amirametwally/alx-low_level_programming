#include "main.h"
/**
 * _abs - calculate the absolute value of number from 0.
 * @i: integer  be converted.
 * Return: absolute value of number
 */
int _abs(int i)
{
	if (i < 0)
		i = i * -1;
	return (i);
}
